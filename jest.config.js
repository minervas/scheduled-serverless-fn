module.exports = {
  // indicate to jest that we are testing a node.js project
  testEnvironment: 'node',
  // tell jest that tests will be in the __tests__ directory and will be named with a .test.js
  // suffex
  testMatch: ['**/__tests__/**/*.test.js'],
};
